{
  "name": "seoulsilk-checkout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Stripe Checkout worker for the Seoulsilk store",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format pretty",
    "secrets": "echo 'wrangler secret put STRIPE_SECRET_KEY && wrangler secret put STRIPE_WEBHOOK_SECRET'"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
